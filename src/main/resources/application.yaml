spring:
  main:
    show-banner: false

  thymeleaf:
    cache: false

endpoints:
  health:
    sensitive: false

server:
  port: ${port:8090}

management:
  context-path: /editar

flags:
  git.push: false
  esquentar.cache: true
  importar: true

eds:
  cartas:
    repositorio: "https://github.com/servicosgovbr/cartas-de-servico.git"